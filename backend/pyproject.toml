# BUILD SYSTEM CONFIGS #
[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
include = ["kgfegmcp*"]
where = ["src"]

# TOOLS #

# black
[tool.black]
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
setup.py
'''
include = '\.pyi?$'
line-length = 88

# interrogate
[tool.interrogate]
badge-format = "svg"
color = true
exclude = ["*/__init__.py"]
ext = []
generate-badge = "../"  # Output file location
ignore-magic = false
ignore-init-method = false
ignore-init-module = false
ignore-private = false
ignore-property-decorators = false
ignore-module = false
ignore-nested-classes = false
ignore-nested-functions = false
ignore-overloaded-functions = false
ignore-semiprivate = false
ignore-setters = false
ignore-regex = ["__init__.py*", "^mock_.*"]
fail-under = 100
omit-covered-files = false
quiet = false
verbose = 0  # Possible values: 0 (minimal output), 1 (-v), 2 (-vv)
whitelist-regex = []

# isort
[tool.isort]
atomic = true
filter_files = true
import_heading_firstparty = "Package Library"
import_heading_future = "Future Library"
import_heading_localfolder = "Local Folder"
import_heading_stdlib = "Standard Library"
import_heading_thirdparty = "Third Party Library"
include_trailing_comma = true
known_first_party = "kgfegmcp"
known_third_party = "git, mpi4py, ntpath, numpy, pexpect, pytest, sphinx_rtd_theme, yaml"
line_length = 88
lines_between_types = 1
multi_line_output = 3
profile = "black"

# mypy
[tool.mypy]
disallow_untyped_defs = true
ignore_missing_imports = true
plugins = ["pydantic.mypy"]

# pytest
[tool.pytest.ini_options]
asyncio_mode = "auto"
bdd_features_base_dir = "tests/features/"
markers = ["costs-money"]
pythonpath = [".", "kgfegmcp"]
testpaths = ["tests"]

# pytest coverage
[tool.coverage.paths]
kgfegmcp = ["backend/src", "src"]

[tool.coverage.report]
exclude_lines = [
    'if __name__ == "__main__"',
    "if __name__ == '__main__'",
    "def cli",
    "if TYPE_CHECKING:",
    "pragma: no cover",
]
omit = ["./src/kgfegmcp/__init__.py"]

[tool.coverage.run]
branch = true
concurrency = ["greenlet", "thread"]
omit = ["*/tests/*", "*/__init__.py"]
source = ["kgfegmcp"]

# ruff
[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint.mccabe]
max-complexity = 10

# PROJECT METADATA #
[project]
authors = [{name = "IDinsight DSEM", email = "dsem@idinsight.org" }]
classifiers = [
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.13",
]
description = "Knowledge Graph For Education Global MCP"
keywords = ["education", "dag", "genai", "llm", "agents", "knowledge graph"]
maintainers = [{name = "IDinsight DSEM", email = "dsem@idinsight.org" }]
name = "kgfegmcp"
readme = "README.md"
requires-python = ">=3.13,<3.14"
version = "0.1.0"
dependencies = [
    "colored>=2.3.1",
    "fastmcp==3.4.4",
    "loguru>=0.7.3",
    "platformdirs>=4.9.2",
    "pydantic>=2.11.9",
    "pydantic-settings>=2.11.0",
    "PyYAML>=6.0.3",
    "termcolor>=3.1.0",
    "typer>=0.19.2",
]

[project.optional-dependencies]
dev = [
    "black==25.9.0",
    "detect-secrets==1.5.0",
    "interrogate==1.7.0",
    "isort==6.1.0",
    "mypy==1.18.2",
    "mypy-extensions==1.1.0",
    "pre-commit==4.3.0",
    "pylint==3.3.8",
    "pylint-pytest==1.1.8",
    "pytest==8.2.0",
    "pytest-asyncio==1.2.0",
    "pytest-bdd==8.1.0",
    "pytest-cov==7.0.0",
    "pytest-mock==3.15.1",
    "pytest-order==1.3.0",
    "pytest-randomly==4.0.1",
    "pytest-xdist==3.8.0",
    "ruff==0.13.3",
    "types-aiofiles==24.1.0.20250822",
    "types-requests==2.32.4.20260107",
]
docs = [
    "mkdocs>=1.6.1",
    "mkdocs-glightbox>=0.4.0",
    "mkdocs-material>=9.6.12",
    "mkdocs-material-extensions>=1.3.1",
    "mkdocs-open-in-new-tab>=1.0.3",
    "pymdown-extensions>=10.9"
]

[project.scripts]
kgfegmcp-build-manifest = "kgfegmcp.cli.build_manifests:main"
